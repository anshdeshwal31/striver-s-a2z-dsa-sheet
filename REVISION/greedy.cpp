#include <bits/stdc++.h>
using namespace std;

bool checkValidString(string s) {
    int open = 0;
    int star = 0 ;
    for(int i = 0; i < s.size(); i++){
        if(s[i]=='('){
            
        }
    }
}